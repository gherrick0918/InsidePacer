package app.insidepacer.backup

/** Feature flag toggle for Google Drive backup/sign-in support. */
object BackupFeatureConfig {
    /** Enable to surface the backup UI and functionality. */
    const val enabled: Boolean = true
}
